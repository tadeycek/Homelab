# Copy to .env and fill in real values. NEVER commit .env.

# CurseForge API key (only needed for CurseForge modpacks)
CF_API_KEY=

# Comma-separated Minecraft usernames allowed to join (whitelist is enforced)
MC_WHITELIST=
